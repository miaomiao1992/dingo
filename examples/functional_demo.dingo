package main

import "fmt"

func main() {
	var numbers []int
	numbers = []int{1, 2, 3, 4, 5}

	var doubled []int
	doubled = numbers.map(func(x int) int { return x * 2 })

	fmt.Println("Doubled:")
	fmt.Println(doubled)
}
