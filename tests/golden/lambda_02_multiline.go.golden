package main

func main() {

	process := func(x int, y int) int {
		sum := x + y
		doubled := sum * 2
		return doubled
	}

	max := func(a int, b int) int {
		if a > b {
			return a
		}
		return b
	}

	result := process(5, 3)
	maxVal := max(10, 20)
	println("Result:", result, "Max:", maxVal)
}
