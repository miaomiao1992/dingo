package main

import (
	"os"
	"strconv"
)

func processData(path string) (int, error) {
	tmp1, err1 := os.ReadFile(path)

	if err1 != nil {
		return 0, err1
	}
	// dingo:e:1
	var data = tmp1
	tmp2, err2 := strconv.Atoi(string(data))

	if err2 != nil {
		return 0, err2
	}

	return tmp2, nil
}
