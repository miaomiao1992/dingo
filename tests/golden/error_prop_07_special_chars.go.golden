package main

import (
	"fmt"
	"os"
)

func readWithMessage(path string) ([]byte, error) {
	tmp, err := os.ReadFile(path)
	// dingo:e:0
	if err != nil {
		return nil, fmt.Errorf("failed to read \"important\" file: %w", err)
	}
	var data = tmp
	return data, nil
}
