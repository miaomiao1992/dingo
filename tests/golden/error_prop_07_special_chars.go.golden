package main

import (
	"fmt"
	"os"
)

func readWithMessage(path string) ([]byte, error) {
	tmp1, err1 := os.ReadFile(path)

	if err1 != nil {
		return nil, fmt.Errorf("failed to read \"important\" file: %w", err1)
	}
	// dingo:e:1
	var data = tmp1
	return data, nil
}
