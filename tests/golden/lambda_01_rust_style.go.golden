package main

func main() {
	// Rust-style lambda syntax
	var add = func(a, b interface{}) interface{} {
		return a + b
	}
	var double = func(x interface{}) interface{} {
		return x * 2
	}
	var greet = func(name interface{}) interface{} {
		return "Hello, " + name
	}
}
