package main

import (
	"encoding/json"
	"fmt"
	"os"
)

type Config struct {
	Host string
	Port int
}

func pipeline(path string) (*Config, error) {
	tmp1, err1 := os.ReadFile(path)

	if err1 != nil {
		return nil, fmt.Errorf("failed to read config: %w", err1)
	}
	// dingo:e:1
	var data = tmp1
	var cfg Config
	tmp2, err2 := json.Unmarshal(data, &cfg)

	if err2 != nil {
		return nil, fmt.Errorf("failed to parse config: %w", err2)
	}
	// dingo:e:1
	var err = tmp2
	return &cfg, nil
}
