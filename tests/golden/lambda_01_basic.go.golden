package main

func main() {
	// Rust-style lambda syntax with explicit types
	var ad d = func(a int, b int) int { a + b }
	var doubl e = func(x int) int { x * 2 }
	var gree t = func(name string) string { "Hello, " + name }
}
