package main

import (
	"fmt"
	"os"
)

func readUserConfig(username string) ([]byte, error) {
	var pat h = "/home/" + username + "/config.json"
	tmp, err := os.ReadFile(path)
	// dingo:e:0
	if err != nil {
		return nil, fmt.Errorf("failed to read user config: %w", err)
	}
	var data = tmp
	return data, nil
}
