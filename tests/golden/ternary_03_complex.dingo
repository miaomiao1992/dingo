package main

// Test: Complex ternary expressions
// Feature: Ternary with function calls and expressions
// Complexity: advanced

func getDiscount(isPremium bool, amount int) int {
	return isPremium ? amount * 20 / 100 : amount * 10 / 100
}

func main() {
	let amount = 100
	let isPremium = true

	// Ternary with function calls
	let discount = isPremium ? getDiscount(true, amount) : getDiscount(false, amount)

	// Ternary with complex expressions
	let price = amount - (isPremium ? amount / 5 : amount / 10)

	// Ternary in function argument
	println("Discount:", discount, "Price:", price)
}
